// import { useRouter } from "next/navigation";

// const YourComponent = ({  row }) => {
//   const router = useRouter();

//   const handleNavigation = (row) => {
//     // Define the actual path you want to navigate to
//     const path = `/companies/Components/previewCompany?pageId=${row.original.id}`;
//     // Define the new URL you want to display in the address bar
//     const newUrl = `/your-custom-url/${row.original.id}`; // Change as needed

//     // Navigate to the actual path
//     router.push(path);
    
//     // Change the URL in the address bar
//     window.history.pushState({}, '', newUrl);
//   };

  
// };

// export default YourComponent;
